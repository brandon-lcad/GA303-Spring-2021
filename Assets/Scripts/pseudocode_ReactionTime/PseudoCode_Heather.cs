using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PseudoCode_Heather : MonoBehaviour
{
    //Introduce the game and rules

    //IF player clicks the screen, set countdown to random number within desired range
        //Change Screen to Red
        //Start countdown
        //Display wait text

    //IF player clicks on red, display failure text
    //ELSE when countdown ends, change screen to green
        //IF player clicks on green, stop reaction timer
            //Display reaction timer value

    //IF player clicks again, return to first IF loop
}
