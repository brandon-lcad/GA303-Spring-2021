using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pseudocode_AbdallaT : MonoBehaviour
    //welcome intro and instructions
    //click to start 
    //red Screen with WAIT text on it 
    //wait a RANDOM amount of seconds
    //screen goes to GREEN with TAP text on it
    //count seconds it took the player to tap 
    //stop the timer when player taps
    //save the timer amount in value x
    //present a screen with value x as the reaction time
    //player taps again for next screen
    //give instructions to click again to play
    //loop back to red screen
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
