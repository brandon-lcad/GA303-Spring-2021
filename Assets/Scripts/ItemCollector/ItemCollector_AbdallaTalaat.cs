using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemCollector_AbdallaTalaat : MonoBehaviour
{
    //Inside unity create the objects, objects named: Obx (collectables)
    //Set up the scored board on the camera to be updated
    //name the object Obx and keep it as a public value in unity
    //Score that needs to be updated
    //Add sound file of the object getting caught

    //Variables to add:
    //Obx (Object)
    //Sound trigger
    //Score board

    //if player collides with object sound is produced 
    //and object is removed from scene
    //if player collides with object the score boar is updated

    // Start is called before the first frame update

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
