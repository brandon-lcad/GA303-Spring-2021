using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PseudoCode_ChangTse : MonoBehaviour
{
    //Introduce the game
    //Explain the rules
        //IF player clicks mouse (Start Countdown timer == TT)(change screen to red)
            //i = generate random number
            //IF (TT <= i and Player clicks mouse) 
                //Post "clicked to soon"
                //Start again
        //IF TT = i (change screen to green)(Start reaction Timer == RT)
        //IF player clicks mouse (Stop RT) (Post RT)


}
