using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pseudocode_AshlynD : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Introduce the game and the option/rules
        
        //open to main menu
        //if player clicks on high scores - send to high score screen
        //if player click on start game - start the game - add thirty seconds to timer
        //add text "click the duck" in game
        //add timer countdown
        //if clicks on object add plus one to click counter
        //if player uses left mouse button on object - happy quack noise
        //if player uses left mouse button not on object - angry quack noise
        //if player uses left mouse button 20 times in 1 second - spray confetti
        //on end time add screenpop with successful click done up and freeze game
        //if player clicks button "highcore page" switch scenes
        //new screen highscore page log 5 most successful games
        //if player hits mainmenu send to main menu

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
