using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PseudoCode_Hikari : MonoBehaviour
{
    // Introduce the game and explain the rules
        // IF player clicks mouse
          // Start countdown timer with random set time
          // Set screen to red
          // PRINT "Click when screen turns green"
          // IF player clicks before countdown timer is up
                // PRINT "Clicked too soon"
                // PRINT "Click again on screen to start game again"
                    // IF player clicks, return to beginning
          // IF countdown timer runs out
            // Set screen to green
            // PRINT "click now"
                // IF player clicks after countdown timer is up
                    // Stop timer
                    // PRINT time between turning green and click
                    // PRINT "CLick to keep going"
                        // IF player clicks, return to beginning
}
