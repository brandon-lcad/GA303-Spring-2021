using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PseudoCode_RhenLundell : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Welcome Statement
        //Explanation of the controls, ex: press spacebar when you hear a noise.
        //the test will begin once you press enter. 

    }

    // Update is called once per frame
    void Update()
    {
        //if enter is pressed, start timer
        //after a random interval of seconds play a sound
        //if spacebar is pressed, record when
        //subtract the time when the sound was played from the time it took the player to react
        //loop game to start again upon pressing enter
    }

    void End()
    {
        //if game is played 5 times, present median reaction time to player
    }
}
